# print-agent (Windows) — variables de entorno (F10.4).
# El agente corre local en el POS; normalmente no necesita .env (usa defaults).

# Directorio de datos (default: %PROGRAMDATA%\CyberposPrintAgent)
PRINT_AGENT_DATA_DIR=
